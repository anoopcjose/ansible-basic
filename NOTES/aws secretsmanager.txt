curl "https://awscli.amazonaws.com/awscli-exe-linux-x86_64.zip" -o "awscliv2.zip"
unzip awscliv2.zip
sudo ./aws/install

To update your current installation of the AWS CLI, add your existing symlink and installer information to construct the install command with the --update parameter.

sudo ./aws/install --bin-dir /usr/local/bin --install-dir /usr/local/aws-cli --update

$ which aws
/usr/local/bin/aws

 aws secretsmanager --region=ap-south-1 get-secret-value --secret-id gioapi | grep  "SecretString"| cut -d: -f3| sed 's/\\"//g'| sed 's/}",//g'
 
 ansible-vault encrypt .yml --vault-password-file=key.sh
 
 #!/bin/bash
 
playbook_secret=$(aws secretsmanager  get-secret-value --secret-id ipgeolocation-api-key --query SecretString --output text | cut -d: -f2 | tr -d \"})

echo $playbook_secret

aws secretsmanager  get-secret-value --secret-id ipgeolocation-api-key --output text --query SecretString | python3 -c 'import json, sys; print(json.load(sys.stdin)["ipgeolocation-api-key"])'

https://www.learnaws.org/2022/08/28/aws-cli-secrets-manager/





