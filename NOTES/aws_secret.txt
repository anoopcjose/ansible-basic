    - name: "Build - Login to Docker-hub Account"
      when: clone_status.changed == true
      docker_login:
        aws_access_key: "{{ access_key }}"
        aws_secret_key: "{{ secret_key }}"
        region: "{{ region }}"
        username: "{{ docker_user }}"
        password: "{{ lookup('amazon.aws.aws_secret', 'docker_password') }}"
        state: present
